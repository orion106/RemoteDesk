<UserControl x:Class="RemoteAssist.InventoryView"
 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
 xmlns:local="clr-namespace:RemoteAssist">
 <UserControl.Resources>
  <Style TargetType="TabItem"><Setter Property="Foreground" Value="{DynamicResource TextBrush}"/><Setter Property="Template"><Setter.Value><ControlTemplate TargetType="TabItem"><Border x:Name="tab" Background="{DynamicResource HoverBrush}" Padding="12,9" Margin="0,0,4,4" CornerRadius="5"><ContentPresenter ContentSource="Header"/></Border><ControlTemplate.Triggers><Trigger Property="IsSelected" Value="True"><Setter TargetName="tab" Property="Background" Value="{DynamicResource AccentSoftBrush}"/></Trigger></ControlTemplate.Triggers></ControlTemplate></Setter.Value></Setter></Style>
  <Style x:Key="InventoryList" TargetType="ListBox"><Setter Property="Background" Value="{DynamicResource InputBrush}"/><Setter Property="Foreground" Value="{DynamicResource TextBrush}"/><Setter Property="BorderBrush" Value="{DynamicResource LineBrush}"/><Setter Property="ScrollViewer.HorizontalScrollBarVisibility" Value="Disabled"/></Style>
  <Style x:Key="InventoryLabel" TargetType="TextBlock"><Setter Property="Margin" Value="0,9,0,4"/><Setter Property="Foreground" Value="{DynamicResource MutedBrush}"/><Setter Property="FontSize" Value="12"/><Setter Property="TextWrapping" Value="Wrap"/></Style>
  <Style x:Key="InventoryField" TargetType="TextBox"><Setter Property="MinHeight" Value="32"/><Setter Property="Padding" Value="8,6"/><Setter Property="Background" Value="{DynamicResource InputBrush}"/><Setter Property="Foreground" Value="{DynamicResource TextBrush}"/><Setter Property="CaretBrush" Value="{DynamicResource TextBrush}"/><Setter Property="BorderBrush" Value="{DynamicResource LineBrush}"/><Setter Property="Template"><Setter.Value><ControlTemplate TargetType="TextBox"><Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="1" CornerRadius="6"><ScrollViewer x:Name="PART_ContentHost"/></Border></ControlTemplate></Setter.Value></Setter></Style>
  <Style TargetType="Expander"><Setter Property="Foreground" Value="{DynamicResource TextBrush}"/></Style>
 </UserControl.Resources>
 <Grid Margin="18,14">
  <Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="*"/><RowDefinition Height="Auto"/></Grid.RowDefinitions>
  <DockPanel Margin="0,0,0,12"><Button DockPanel.Dock="Right" Content="Обновить" Command="{Binding RefreshCommand}"/><StackPanel><TextBlock Text="Техника корпуса" FontSize="22" FontWeight="SemiBold"/><TextBlock Text="{Binding ConnectionStatus}" Foreground="{DynamicResource MutedBrush}" FontSize="12" Margin="0,4,10,0" TextWrapping="Wrap"/></StackPanel></DockPanel>
  <TabControl Grid.Row="1" SelectedIndex="{Binding Tab}" Background="{DynamicResource PanelBrush}" BorderBrush="{DynamicResource LineBrush}">
   <TabItem Header="Карта">
    <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto"><StackPanel Margin="12" MinWidth="650">
     <WrapPanel><TextBlock Text="Этаж" VerticalAlignment="Center" Margin="0,0,8,0"/><ComboBox ItemsSource="{Binding Floors}" SelectedItem="{Binding Floor}" Width="80"/><Button Content="Коридор этажа" Command="{Binding OpenCorridorCommand}" Margin="12,0,0,0"/><TextBlock Text="Нажмите кабинет или коридор на плане" Foreground="{DynamicResource MutedBrush}" VerticalAlignment="Center" Margin="16,0,0,0"/></WrapPanel>
     <Expander Header="План этажа" IsExpanded="True"><local:FloorPlanControl x:Name="FloorPlan" Floor="{Binding Floor}" SelectedNumber="{Binding SelectedRoomNumber}" Height="205" Margin="0,10,0,12"/></Expander>
     <Grid><Grid.ColumnDefinitions><ColumnDefinition Width="185"/><ColumnDefinition Width="12"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions>
      <StackPanel><TextBlock Text="Помещения этажа" FontWeight="SemiBold" Margin="0,0,0,7"/><ListBox ItemsSource="{Binding FloorRooms}" SelectedItem="{Binding SelectedRoom}" Height="220" Style="{StaticResource InventoryList}"><ListBox.ItemTemplate><DataTemplate><StackPanel Margin="4"><TextBlock Text="{Binding Number}" FontWeight="SemiBold"/><TextBlock Text="{Binding Name}" FontSize="11" TextWrapping="Wrap"/></StackPanel></DataTemplate></ListBox.ItemTemplate></ListBox>
       <Expander Header="Карточка кабинета" Margin="0,12,0,0"><StackPanel><TextBlock Text="Корпус / этаж" Style="{StaticResource InventoryLabel}"/><TextBox Text="{Binding DraftRoom.Building,UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource InventoryField}"/><TextBox Text="{Binding DraftRoom.Floor,UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource InventoryField}" Margin="0,4,0,0"/><TextBlock Text="Номер" Style="{StaticResource InventoryLabel}"/><TextBox Text="{Binding DraftRoom.Number,UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource InventoryField}"/><TextBlock Text="Название" Style="{StaticResource InventoryLabel}"/><TextBox Text="{Binding DraftRoom.Name,UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource InventoryField}"/><Button Content="Новый кабинет" Command="{Binding NewRoomCommand}" Margin="0,8,0,4"/><Button Content="Сохранить кабинет" Command="{Binding SaveRoomCommand}"/></StackPanel></Expander>
      </StackPanel>
      <StackPanel Grid.Column="2"><TextBlock Text="{Binding RoomTitle}" FontWeight="SemiBold" FontSize="16" Margin="0,0,0,8"/><WrapPanel Margin="0,0,0,8"><Button Content="+ Добавить технику" Command="{Binding NewAssetCommand}" Margin="0,0,8,8"/><Button Content="Карточка" Command="{Binding OpenAssetCommand}" Margin="0,0,8,8"/><Button Content="Связать устройства" Command="{Binding BeginLinkCommand}" Margin="0,0,8,8"/><Button Content="Убрать связь" Command="{Binding UnlinkAssetCommand}" Margin="0,0,8,8"/></WrapPanel><TextBlock Text="{Binding SelectionHint}" Foreground="{DynamicResource MutedBrush}" TextWrapping="Wrap" Margin="0,0,0,8"/><WrapPanel Margin="0,0,0,8"><TextBlock Text="Масштаб" VerticalAlignment="Center"/><Slider x:Name="RoomZoom" Minimum="0.6" Maximum="2.5" Value="1" Width="130" Margin="10,0"/><Button Content="Разместить выбранное" Click="PlaceSelected_Click" IsEnabled="{Binding CanEdit}" Padding="10,6"/><Button Content="Убрать со схемы" Command="{Binding ClearPlacementCommand}" Margin="6,0,0,0" Padding="10,6"/></WrapPanel>
       <ScrollViewer Height="340" HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto"><local:RoomPlacementControl x:Name="RoomPlan" Width="660" Height="315"><local:RoomPlacementControl.LayoutTransform><ScaleTransform ScaleX="{Binding Value,ElementName=RoomZoom}" ScaleY="{Binding Value,ElementName=RoomZoom}"/></local:RoomPlacementControl.LayoutTransform></local:RoomPlacementControl></ScrollViewer>
       <TextBlock Text="Не расставлено — выберите объект и нажмите «Разместить выбранное»" Foreground="{DynamicResource MutedBrush}" FontSize="12" TextWrapping="Wrap" Margin="0,10,0,6"/>
       <ListBox ItemsSource="{Binding UnplacedAssets}" SelectedItem="{Binding SelectedAsset}" DisplayMemberPath="Title" Height="115" Style="{StaticResource InventoryList}"/>
      </StackPanel>
     </Grid>
    </StackPanel></ScrollViewer>
   </TabItem>
   <TabItem Header="Техника">
    <Grid Margin="12"><Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="*"/></Grid.RowDefinitions>
     <WrapPanel Margin="0,0,0,10"><Button Content="+ Добавить технику" Command="{Binding NewAssetCommand}" Margin="0,0,8,6"/><Button Content="Открыть карточку" Command="{Binding OpenAssetCommand}" Margin="0,0,8,6"/><TextBox Width="230" Tag="Номер, модель, компьютер…" Text="{Binding Search,UpdateSourceTrigger=PropertyChanged}"/><CheckBox Content="Все кабинеты" IsChecked="{Binding AllRooms}" Margin="12,0" VerticalAlignment="Center"/><CheckBox Content="Показать архив" IsChecked="{Binding IncludeArchived}" VerticalAlignment="Center"/><Button Content="В Excel" Command="{Binding ExportCommand}" Margin="12,0,0,0"/></WrapPanel>
     <Grid Grid.Row="1">
      <DataGrid MouseDoubleClick="Assets_DoubleClick" ItemsSource="{Binding Assets}" SelectedItem="{Binding SelectedAsset}" AutoGenerateColumns="False" IsReadOnly="True"><DataGrid.Columns><DataGridTextColumn Header="Кабинет" Binding="{Binding RoomText}" Width="82"/><DataGridTextColumn Header="Техника" Binding="{Binding Title}" Width="*" MinWidth="135"/><DataGridTextColumn Header="Инв. номер" Binding="{Binding Value.InventoryNumber}" Width="125"/><DataGridTextColumn Header="Телефон" Binding="{Binding Value.PhoneNumber}" Width="125"/><DataGridTextColumn Header="Место / ПК" Binding="{Binding Value.Hostname}" Width="110"/><DataGridTextColumn Header="Состояние" Binding="{Binding Value.Status}" Width="110"/></DataGrid.Columns></DataGrid>

     </Grid>
    </Grid>
   </TabItem>
   <TabItem Header="Картриджи">
    <ScrollViewer Margin="12" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto"><Grid MinWidth="680" Width="{Binding ViewportWidth,RelativeSource={RelativeSource AncestorType=ScrollViewer}}"><Grid.ColumnDefinitions><ColumnDefinition Width="*" MinWidth="330"/><ColumnDefinition Width="18"/><ColumnDefinition Width="310"/></Grid.ColumnDefinitions>
     <StackPanel><WrapPanel Margin="0,0,0,10"><ComboBox ItemsSource="{Binding CartridgeGroups}" SelectedItem="{Binding CartridgeGroup}" Width="190"/><TextBlock Text="{Binding CartridgeGroupSummary}" Margin="12,0" VerticalAlignment="Center"/></WrapPanel><TextBlock Text="Картриджи и их размещение" FontSize="16" FontWeight="SemiBold" Margin="0,0,0,10"/><DataGrid ItemsSource="{Binding FilteredCartridges}" SelectedItem="{Binding SelectedCartridge}" AutoGenerateColumns="False" IsReadOnly="True" Height="300"><DataGrid.Columns><DataGridTextColumn Header="Номер" Binding="{Binding Number}" Width="90"/><DataGridTextColumn Header="Модель" Binding="{Binding Model}" Width="*"/><DataGridTextColumn Header="Цвет" Binding="{Binding Color}" Width="85"/><DataGridTextColumn Header="Состояние" Binding="{Binding Status}" Width="100"/><DataGridTextColumn Header="Позиция" Binding="{Binding Slot}" Width="90"/></DataGrid.Columns></DataGrid>
      <Expander Header="Установка и замена в принтере" Margin="0,14,0,0"><StackPanel><TextBlock Text="Принтер / МФУ" Style="{StaticResource InventoryLabel}"/><ComboBox ItemsSource="{Binding Printers}" SelectedItem="{Binding SelectedPrinter}"><ComboBox.ItemTemplate><DataTemplate><TextBlock><Run Text="{Binding Model}"/><Run Text=" · "/><Run Text="{Binding InventoryNumber}"/></TextBlock></DataTemplate></ComboBox.ItemTemplate></ComboBox>
      <TextBlock Text="{Binding InstalledCartridges}" TextWrapping="Wrap" Margin="0,8"/><TextBlock Text="Позиция / цвет" Style="{StaticResource InventoryLabel}"/><TextBox Text="{Binding CartridgeSlot,UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource InventoryField}"/>
      <TextBlock Text="Устанавливаемый картридж" Style="{StaticResource InventoryLabel}"/><ComboBox ItemsSource="{Binding SpareCartridges}" SelectedItem="{Binding ReplacementCartridge}" DisplayMemberPath="Number"/>
      <TextBlock Text="Состояние снятого картриджа" Style="{StaticResource InventoryLabel}"/><ComboBox ItemsSource="{Binding CartridgeStates}" SelectedItem="{Binding RemovedCartridgeStatus}"/>
      <WrapPanel Margin="0,10,0,0"><Button Content="Установить / заменить" Command="{Binding ReplaceCartridgeCommand}"/><Button Content="Снять" Command="{Binding RemoveCartridgeCommand}" Margin="6,0,0,0"/></WrapPanel></StackPanel></Expander>
     </StackPanel>
     <StackPanel Grid.Column="2"><WrapPanel><Button Content="Новый" Command="{Binding NewCartridgeCommand}"/><Button Content="Сохранить" Command="{Binding SaveCartridgeCommand}" Margin="6,0,0,0"/></WrapPanel><TextBlock Text="Учётный номер экземпляра" Style="{StaticResource InventoryLabel}"/><TextBox Text="{Binding DraftCartridge.Number,UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource InventoryField}"/><TextBlock Text="Модель" Style="{StaticResource InventoryLabel}"/><TextBox Text="{Binding DraftCartridge.Model,UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource InventoryField}"/><TextBlock Text="Цвет" Style="{StaticResource InventoryLabel}"/><TextBox Text="{Binding DraftCartridge.Color,UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource InventoryField}"/><TextBlock Text="Состояние (установку меняйте через замену)" Style="{StaticResource InventoryLabel}"/><ComboBox ItemsSource="{Binding CartridgeStates}" IsEditable="True" Text="{Binding DraftCartridge.Status,UpdateSourceTrigger=PropertyChanged}"/><TextBlock Text="Примечание" Style="{StaticResource InventoryLabel}"/><TextBox Text="{Binding DraftCartridge.Notes,UpdateSourceTrigger=PropertyChanged}" AcceptsReturn="True" TextWrapping="Wrap" MinHeight="90"/><TextBlock Text="История замен — во вкладке «Фото и история» после выбора картриджа." Foreground="{DynamicResource MutedBrush}" TextWrapping="Wrap" Margin="0,14,0,0"/></StackPanel>
    </Grid></ScrollViewer>
   </TabItem>
   <TabItem Header="Проверки">
    <ScrollViewer Margin="12" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto"><StackPanel MinWidth="650" Width="{Binding ViewportWidth,RelativeSource={RelativeSource AncestorType=ScrollViewer}}">
     <WrapPanel><TextBox Text="{Binding AuditName}" Width="220" Tag="Название проверки"/><DatePicker SelectedDate="{Binding AuditDate}" Width="130" Margin="6,0"/><ComboBox ItemsSource="{Binding Scopes}" SelectedItem="{Binding AuditScope}" Width="125"/><Button Content="Начать" Command="{Binding CreateAuditCommand}" Margin="6,0,0,0"/></WrapPanel>
     <TextBlock Text="Кабинет и этаж для новой проверки выбираются на карте." Foreground="{DynamicResource MutedBrush}" FontSize="12" Margin="0,7,0,12"/>
     <ComboBox ItemsSource="{Binding Audits}" SelectedItem="{Binding SelectedAudit}"><ComboBox.ItemTemplate><DataTemplate><TextBlock><Run Text="{Binding Name}"/><Run Text=" · "/><Run Text="{Binding Date,StringFormat=dd.MM.yyyy}"/><Run Text=" · завершена: "/><Run Text="{Binding CompletedUtc,StringFormat=dd.MM.yyyy HH:mm}"/></TextBlock></DataTemplate></ComboBox.ItemTemplate></ComboBox>
     <DataGrid ItemsSource="{Binding AuditItems}" SelectedItem="{Binding SelectedAuditItem}" AutoGenerateColumns="False" IsReadOnly="True" Height="255" Margin="0,10"><DataGrid.Columns><DataGridTextColumn Header="Тип" Binding="{Binding AssetSnapshot.Type}" Width="130"/><DataGridTextColumn Header="Модель" Binding="{Binding AssetSnapshot.Model}" Width="*"/><DataGridTextColumn Header="Инв. номер" Binding="{Binding AssetSnapshot.InventoryNumber}" Width="120"/><DataGridTextColumn Header="Ожидалось" Binding="{Binding ExpectedRoomNumber}" Width="90"/><DataGridTextColumn Header="Найдено" Binding="{Binding ActualRoomNumber}" Width="90"/><DataGridTextColumn Header="Результат" Binding="{Binding Result}" Width="175"/></DataGrid.Columns></DataGrid>
     <Grid IsEnabled="{Binding AuditOpen}"><Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="12"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions><StackPanel><TextBlock Text="Результат проверки" Style="{StaticResource InventoryLabel}"/><ComboBox ItemsSource="{Binding AuditResults}" SelectedItem="{Binding DraftAuditItem.Result,UpdateSourceTrigger=PropertyChanged}"/><TextBlock Text="Фактический кабинет" Style="{StaticResource InventoryLabel}"/><ComboBox ItemsSource="{Binding Rooms}" SelectedValuePath="Id" DisplayMemberPath="Number" SelectedValue="{Binding DraftAuditItem.ActualRoomId,UpdateSourceTrigger=PropertyChanged}"/></StackPanel><StackPanel Grid.Column="2"><TextBlock Text="Комментарий" Style="{StaticResource InventoryLabel}"/><TextBox Text="{Binding DraftAuditItem.Comment,UpdateSourceTrigger=PropertyChanged}" TextWrapping="Wrap" AcceptsReturn="True" MinHeight="90"/></StackPanel></Grid>
     <WrapPanel Margin="0,12,0,0"><Button Content="Сохранить результат" Command="{Binding SaveAuditItemCommand}" IsEnabled="{Binding AuditOpen}"/><Button Content="Применить перемещение" Command="{Binding ApplyAuditMoveCommand}" Margin="6,0,0,0"/><Button Content="Добавить выбранную технику" Command="{Binding AuditAddAssetCommand}" Margin="6,0,0,0" IsEnabled="{Binding AuditOpen}"/></WrapPanel>
     <TextBlock Text="Неучтённую технику сначала сохраните во вкладке «Техника», затем добавьте её в проверку. Фото результата — во вкладке «Фото и история», объект «Проверка»." Foreground="{DynamicResource MutedBrush}" FontSize="12" TextWrapping="Wrap" Margin="0,8,0,12"/>
     <WrapPanel><Button Content="Завершить проверку" Command="{Binding CompleteAuditCommand}" IsEnabled="{Binding AuditOpen}"/><Button Content="Результаты в Excel" Command="{Binding ExportAuditCommand}" Margin="6,0,0,0"/></WrapPanel>
    </StackPanel></ScrollViewer>
   </TabItem>
   <TabItem Header="Фото и история">
    <ScrollViewer VerticalScrollBarVisibility="Auto"><StackPanel Margin="12">
     <WrapPanel><TextBlock Text="Фото для" VerticalAlignment="Center" Margin="0,0,10,0"/><ComboBox ItemsSource="{Binding PhotoScopes}" SelectedItem="{Binding PhotoScope}" Width="170"/><Button Content="Добавить JPG / PNG" Command="{Binding UploadPhotoCommand}" Margin="10,0,0,0"/></WrapPanel>
     <ListBox ItemsSource="{Binding Photos}" SelectedItem="{Binding SelectedPhoto}" Height="205" Style="{StaticResource InventoryList}" Margin="0,12,0,8" ScrollViewer.HorizontalScrollBarVisibility="Auto"><ListBox.ItemsPanel><ItemsPanelTemplate><StackPanel Orientation="Horizontal"/></ItemsPanelTemplate></ListBox.ItemsPanel><ListBox.ItemTemplate><DataTemplate><StackPanel Width="190" Margin="6"><Image Source="{Binding Image}" Height="125" Stretch="Uniform"/><TextBlock Text="{Binding Value.Caption}" TextWrapping="Wrap" Margin="0,6,0,0"/><TextBlock Text="{Binding Value.IsPrimary,StringFormat=Главное фото: {0}}" FontSize="11"/></StackPanel></DataTemplate></ListBox.ItemTemplate></ListBox>
     <TextBox Text="{Binding PhotoCaption,UpdateSourceTrigger=PropertyChanged}" Tag="Подпись к фотографии"/><WrapPanel Margin="0,8,0,16"><Button Content="Сохранить подпись" Command="{Binding SavePhotoCommand}"/><Button Content="Сделать главным" Command="{Binding PrimaryPhotoCommand}" Margin="6,0,0,0"/><Button Content="Увеличить" Command="{Binding ViewPhotoCommand}" Margin="6,0,0,0"/></WrapPanel>
     <TextBlock Text="История выбранной техники, кабинета и картриджа" FontWeight="SemiBold" Margin="0,0,0,10"/><DataGrid ItemsSource="{Binding History}" SelectedItem="{Binding SelectedHistory}" AutoGenerateColumns="False" IsReadOnly="True" Height="230"><DataGrid.Columns><DataGridTextColumn Header="Дата" Binding="{Binding DateUtc,StringFormat=dd.MM.yyyy HH:mm}" Width="145"/><DataGridTextColumn Header="Автор" Binding="{Binding Author}" Width="130"/><DataGridTextColumn Header="Действие" Binding="{Binding Action}" Width="*"/></DataGrid.Columns></DataGrid>
     <Expander Header="Детали события" Margin="0,10,0,0"><TextBox Text="{Binding HistoryDetails,Mode=OneWay}" IsReadOnly="True" AcceptsReturn="True" TextWrapping="Wrap" Height="170" VerticalScrollBarVisibility="Auto"/></Expander>
    </StackPanel></ScrollViewer>
   </TabItem>
   <TabItem Header="Импорт">
    <Grid Margin="12"><Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="*"/><RowDefinition Height="Auto"/></Grid.RowDefinitions>
     <WrapPanel><Button Content="Выбрать XLSX" Command="{Binding ReadImportCommand}"/><Button Content="Перечитать с настройками" Command="{Binding ReReadImportCommand}" Margin="6,0,0,0"/><Button Content="Применить выбранные действия" Command="{Binding ApplyImportCommand}" Margin="6,0,0,0"/></WrapPanel>
     <Expander Grid.Row="1" Header="Сопоставление столбцов" Margin="0,10"><WrapPanel>
      <StackPanel Width="75" Margin="0,0,8,6"><TextBlock Text="Корпус"/><TextBox Text="{Binding ImportOptions.BuildingColumn}" Style="{StaticResource InventoryField}"/></StackPanel><StackPanel Width="75" Margin="0,0,8,6"><TextBlock Text="Кабинет"/><TextBox Text="{Binding ImportOptions.RoomColumn}" Style="{StaticResource InventoryField}"/></StackPanel><StackPanel Width="75" Margin="0,0,8,6"><TextBlock Text="Имя ПК"/><TextBox Text="{Binding ImportOptions.HostnameColumn}" Style="{StaticResource InventoryField}"/></StackPanel><StackPanel Width="75" Margin="0,0,8,6"><TextBlock Text="Тип"/><TextBox Text="{Binding ImportOptions.TypeColumn}" Style="{StaticResource InventoryField}"/></StackPanel><StackPanel Width="75" Margin="0,0,8,6"><TextBlock Text="Модель"/><TextBox Text="{Binding ImportOptions.ModelColumn}" Style="{StaticResource InventoryField}"/></StackPanel><StackPanel Width="75" Margin="0,0,8,6"><TextBlock Text="Инв. №"/><TextBox Text="{Binding ImportOptions.InventoryNumberColumn}" Style="{StaticResource InventoryField}"/></StackPanel><StackPanel Width="75" Margin="0,0,8,6"><TextBlock Text="Описание"/><TextBox Text="{Binding ImportOptions.DescriptionColumn}" Style="{StaticResource InventoryField}"/></StackPanel><StackPanel Width="90" Margin="0,0,8,6"><TextBlock Text="Примечание"/><TextBox Text="{Binding ImportOptions.NotesColumn}" Style="{StaticResource InventoryField}"/></StackPanel><StackPanel Width="65" Margin="0,0,8,6"><TextBlock Text="MAC"/><TextBox Text="{Binding ImportOptions.MacColumn}" Style="{StaticResource InventoryField}"/></StackPanel><StackPanel Width="105" Margin="0,0,8,6"><TextBlock Text="Первая строка"/><TextBox Text="{Binding ImportOptions.FirstDataRow}" Style="{StaticResource InventoryField}"/></StackPanel><StackPanel Width="130"><TextBlock Text="Лист (пусто = все)"/><TextBox Text="{Binding ImportOptions.SheetName}" Style="{StaticResource InventoryField}"/></StackPanel>
     </WrapPanel></Expander>
     <DataGrid x:Name="ImportTable" Grid.Row="2" ItemsSource="{Binding ImportPreview.Rows}" AutoGenerateColumns="False" IsReadOnly="False" CanUserAddRows="False" SelectionUnit="FullRow"><DataGrid.Columns>
      <DataGridTextColumn Header="Строка" Binding="{Binding RowNumber}" IsReadOnly="True" Width="60"/>
      <DataGridTemplateColumn Header="Действие" Width="120"><DataGridTemplateColumn.CellTemplate><DataTemplate><ComboBox ItemsSource="{Binding DataContext.ImportActions,RelativeSource={RelativeSource AncestorType=UserControl}}" DisplayMemberPath="Label" SelectedValuePath="Value" SelectedValue="{Binding Action,UpdateSourceTrigger=PropertyChanged}"/></DataTemplate></DataGridTemplateColumn.CellTemplate></DataGridTemplateColumn>
      <DataGridTemplateColumn Header="Объект для обновления" Width="250"><DataGridTemplateColumn.CellTemplate><DataTemplate><ComboBox ItemsSource="{Binding DataContext.ImportTargets,RelativeSource={RelativeSource AncestorType=UserControl}}" DisplayMemberPath="Summary" SelectedValuePath="Id" SelectedValue="{Binding TargetAssetId,UpdateSourceTrigger=PropertyChanged}" SelectionChanged="ImportTarget_Changed"/></DataTemplate></DataGridTemplateColumn.CellTemplate></DataGridTemplateColumn>
      <DataGridTextColumn Header="Корпус" Binding="{Binding Building}" Width="70"/><DataGridTextColumn Header="Кабинет" Binding="{Binding Room}" Width="90"/><DataGridTextColumn Header="Имя ПК" Binding="{Binding Hostname}" Width="115"/><DataGridTextColumn Header="Тип" Binding="{Binding Type}" Width="130"/><DataGridTextColumn Header="Модель" Binding="{Binding Model}" Width="140"/><DataGridTextColumn Header="Инв. №" Binding="{Binding InventoryNumber}" Width="130"/><DataGridTextColumn Header="Описание" Binding="{Binding Description}" Width="140"/><DataGridTextColumn Header="Примечание" Binding="{Binding Notes}" Width="140"/><DataGridTextColumn Header="MAC" Binding="{Binding Mac}" Width="135"/><DataGridCheckBoxColumn Header="Переместить" Binding="{Binding ApplyLocationChange}" Width="95"/><DataGridTextColumn Header="Замечания" Binding="{Binding IssuesText}" IsReadOnly="True" Width="300"/>
     </DataGrid.Columns></DataGrid>
     <TextBlock Grid.Row="3" Text="{Binding ImportSummary}" TextWrapping="Wrap" Foreground="{DynamicResource MutedBrush}" Margin="0,10,0,0"/>
    </Grid>
   </TabItem>
   <TabItem Header="Подключение">
    <ScrollViewer VerticalScrollBarVisibility="Auto"><StackPanel Margin="18" MaxWidth="740" HorizontalAlignment="Left">
     <TextBlock Text="Общая инвентаризация на TrueNAS" FontSize="18" FontWeight="SemiBold"/><TextBlock Text="Введите адрес установленной службы и личную учётную запись программы." Foreground="{DynamicResource MutedBrush}" TextWrapping="Wrap" Margin="0,7,0,12"/>
     <TextBlock Text="HTTPS-адрес службы" Style="{StaticResource InventoryLabel}"/><TextBox Text="{Binding ServerUrl}" Tag="https://inventory.example:8443/" MinWidth="380"/>
     <TextBlock Text="Логин" Style="{StaticResource InventoryLabel}"/><TextBox Text="{Binding Username}"/>
     <TextBlock Text="Пароль" Style="{StaticResource InventoryLabel}"/><PasswordBox x:Name="LoginPassword" PasswordChanged="LoginPassword_Changed" MinHeight="38" Padding="10" Background="{DynamicResource InputBrush}" Foreground="{DynamicResource TextBrush}"/>
     <WrapPanel Margin="0,12,0,0"><Button Content="Войти" Command="{Binding LoginCommand}" Style="{DynamicResource PrimaryButton}"/><Button Content="Выйти" Command="{Binding LogoutCommand}" Margin="8,0,0,0"/><Button Content="Загрузить кабинеты с планов" Command="{Binding SeedRoomsCommand}" Margin="8,0,0,0"/></WrapPanel>
     <TextBlock Text="База и фотографии хранятся на NAS. При недоступности службы можно просмотреть последнюю загруженную копию. Пароль управления TrueNAS здесь не используется." Foreground="{DynamicResource MutedBrush}" TextWrapping="Wrap" Margin="0,14,0,18"/>
     <Expander Header="Пользователи — доступно владельцу"><StackPanel Margin="0,10,0,0"><Button Content="Загрузить пользователей" Command="{Binding RefreshUsersCommand}" HorizontalAlignment="Left"/><ListBox ItemsSource="{Binding Users}" SelectedItem="{Binding SelectedUser}" DisplayMemberPath="Username" Height="100" Style="{StaticResource InventoryList}" Margin="0,8"/><WrapPanel><Button Content="Новый пользователь" Command="{Binding NewUserCommand}"/><Button Content="Редактировать" Command="{Binding EditUserCommand}" Margin="8,0,0,0"/></WrapPanel><TextBlock Text="Логин" Style="{StaticResource InventoryLabel}"/><TextBox Text="{Binding DraftUser.Username,UpdateSourceTrigger=PropertyChanged}"/><TextBlock Text="Имя в журнале" Style="{StaticResource InventoryLabel}"/><TextBox Text="{Binding DraftUser.DisplayName,UpdateSourceTrigger=PropertyChanged}"/><TextBlock Text="Роль: editor — учёт, owner — управление пользователями" Style="{StaticResource InventoryLabel}"/><ComboBox ItemsSource="{Binding UserRoles}" SelectedItem="{Binding DraftUser.Role,UpdateSourceTrigger=PropertyChanged}"/><CheckBox Content="Отключить учётную запись" IsChecked="{Binding DraftUser.Disabled,UpdateSourceTrigger=PropertyChanged}" Margin="0,10"/><TextBlock Text="Новый пароль (пусто — оставить прежний)" Style="{StaticResource InventoryLabel}"/><PasswordBox PasswordChanged="UserPassword_Changed" MinHeight="38" Padding="10" Background="{DynamicResource InputBrush}" Foreground="{DynamicResource TextBrush}"/><Button Content="Сохранить пользователя" Command="{Binding SaveUserCommand}" Margin="0,10,0,0" HorizontalAlignment="Left"/></StackPanel></Expander>
    </StackPanel></ScrollViewer>
   </TabItem>
  </TabControl>
  <TextBlock Grid.Row="2" Text="{Binding Message}" TextWrapping="Wrap" FontSize="12" Foreground="{DynamicResource MutedBrush}" Margin="0,10,0,0" MaxHeight="80"/>
 </Grid>
</UserControl>
